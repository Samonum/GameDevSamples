### Collision groups, Flags and binary operators

Each of our objects is given a `CollisionGroup` `ownCollisionGroup` that contains that object's group and `collidesWith` that that contains the groups our object interacts with.

```
[Flags]
public enum CollisionGroup
{
    None = 0,
    Bullets = 1,
    Player = 2,
	  Obstacles = 4
}
```

1. When is the following statement true?
`other.ownCollisionGroup & this.collidesWith == CollisionGroup.None` 

2. What if it is false?

3. If we want to add a 4th collision group for pickups, what number would it get?

4. Bullets interact with both the player and obstacles. How do we set CollidesWith to check for both?

5. What would its binary representation be?

6. There is a powerup that temporarily makes the player avoid bullets. How do we remove the `CollisionGroup.Bullet` from `collidesWith`?



### Collision grids

We have a grid with cells of 50x50 pixels, starting at position (0,0).

We have a player sprite at position (235,120) with a with of 100x100 pixels.

1. What grid cells does the player overlap?

2. We have enemies that deal contact damage to the player and are 50x50 pixels. Which of the following enemies share a grid cell with the player?

- Enemy 1: (355, 120)
- Enemy 2: (160, 170)
- Enemy 3: (340, 230)

